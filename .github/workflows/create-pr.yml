name: Auto Create Pull Request

on:
  push:
    branches-ignore:
      - main
      - master

jobs:
  create_pr:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3 # Atualize para a versão mais recente se estiver disponível

      - name: Create PR
        uses: peter-evans/create-pull-request@v4 # Atualize para a versão mais recente se estiver disponível
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          title: "Auto PR from ${{ github.ref }}"
          body: "Automated PR for changes made in ${{ github.ref }}"
